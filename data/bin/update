#!/bin/sh

set -x
set -e

cd ${HOME}/.dotfiles
git fetch
git stash && git checkout master && git reset --hard origin/master

cd ${HOME}/.private-dotfiles
git fetch
git stash && git checkout master && git reset --hard origin/master

${HOME}/bin/dot

boxen
